package Prerano::Examples::PublicFunctions;

public fn id(int $a, float $b) int = $a;

public enum STATUS {
    GOOD,
    BAD
}

type numeric = int|string|float

public fn decode(0|1 $status) int = $status;

public fn decode2(STATUS $status) int = $status;

public fn decode3(numeric $value) int = $value;